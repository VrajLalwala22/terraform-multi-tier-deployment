services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      # Mount the terraform state so it is not lost between container restarts
      - ./terraform:/app/terraform
    environment:
      - NODE_ENV=production
